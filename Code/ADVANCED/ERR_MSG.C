#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
  int error;

  for (error = 0; error < strerror; error++)
    printf("Error %d %s\n", error, strerror[error]);
  return 0;
}
